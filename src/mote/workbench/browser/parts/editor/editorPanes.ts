import { Disposable } from "vs/base/common/lifecycle";
import { EditorInput } from "./editorinput";

export class EditorPanes extends Disposable {
    
    async openEditor(editor: EditorInput) {
        
    }

    doOpenEditor() {
        
    }
}